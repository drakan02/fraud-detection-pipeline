apiVersion: v1
kind: Secret
metadata:
  name: fraud-pipeline-secrets
type: Opaque
stringData:
  # ClickHouse credentials — shared by Flink, ML Server, and ClickHouse itself
  CLICKHOUSE_USER: "default"
  CLICKHOUSE_PASSWORD: "clickhousepass"
